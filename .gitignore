# local state — every install regenerates these
.workloop/
workloop.config.json

# editor / agent scratch
.claude/

# os + tooling noise
.DS_Store
node_modules/
*.log
.env
.env.*

# transient memory write artifacts (CLAUDE.md is committed; its lock/tmp are not)
CLAUDE.md.lock
*.tmp
